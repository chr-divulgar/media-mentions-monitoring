# Frontend (web-ui)

Este frontend se construye con Vite y React. El resultado del build (carpeta `dist`) se copia a `apps/web-api/public` y es servido automáticamente por el backend NestJS.

No es necesario desplegar el frontend por separado en producción.
